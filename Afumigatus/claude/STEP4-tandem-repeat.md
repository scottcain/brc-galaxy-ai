# Step 3:  Tandem Repeat Detection

## Overview

This step should be fairly short: we will use the BAM files generated during SNP calling to look for a tandem repeat in the cyp51A promoter.

## Tasks 

- Using the MCP server set up in Step 1, extract cyp51A promoter region reads from the BAMs produced in Step 2. All steps should be performed in Galaxy.
- Do tandem repeat finding, presumably using TRF but if necessary, examine other methods. Try to avoid scripting, but should scripting be deemed necessary, it should be done via JupyterLite in Galaxy.

### Iteration 1

- Adding a reminder to use MCP server and, at least in this step, avoid scripting, but if scripting is required, it should be as JupyterLite in Galaxy.
